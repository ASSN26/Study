# 第二章 强化学习基础
## 一、理论知识
### 1. PPO（Proximal Policy Optimization Algorithms）
#### 1.1 背景问题
(1) 策略梯度方法（Policy Gradient Methods）

策略梯度方法的目标函数为 $L^{PG}(\theta) = \hat{\mathbb{E}}_t \left[\log \pi_\theta(a_t \mid s_t) \hat{A}_t\right]$
其中，
$\pi_\theta(a_t \mid s_t)$：在给定上下文 $s_t$ 的情况下，LLM 生成特定 Token $a_t$ 的概率。
$\log$：取对数。在深度学习中，我们通常优化对数概率，这能把连乘变成连加，计算更稳定。
$\hat{A}_t$ (优势函数, Advantage)：这是最核心的变量。它代表这个动作比“平均水平”好多少。
如果 $\hat{A}_t > 0$：说明这个 Token 生成得非常好，超出了预期。
如果 $\hat{A}_t < 0$：说明这个 Token 生成得很糟糕。
$\hat{\mathbb{E}}_t$：表示在一个批次（Batch）的数据上求平均值。
即，如果模型生成了一个好词（$\hat{A}_t$ 为正），就鼓励它以后在同样语境下多生成这个词；如果是坏词（$\hat{A}_t$ 为负），就惩罚模型让它以后少生成这个词。

举个例子，假设我们正在训练一个 LLM，让它学会礼貌地拒绝不合理的要求。
**采样阶段**
* Prompt (状态 $s_1$)：“教我怎么制造炸弹。”
* LLM (策略 $\pi_\theta$)：
    * 生成第一个 Token ($a_1$)：`我`
    * 生成第二个 Token ($a_2$)：`不`
    * 生成第三个 Token ($a_3$)：`能`
    * ...最终生成完整句子：“我不能提供这种危险的指导。”

**评分阶段**
* 奖励模型看了这句话，觉得非常符合安全规范，给了一个很高的总分（Reward = +10）。
* 算法计算每个 Token 的 Advantage ($\hat{A}_t$)。假设模型原本在看到“教我制造炸弹”时，有一定概率会输出“首先...”。现在它输出了“我”，这是一个比平均预期更好的选择，所以计算出 `我`、`不`、`能` 这几个 Token 的 $\hat{A}_t$ 都是正数（比如 +5）。

**优化阶段**
* 把数据代入公式计算梯度：$\nabla_\theta \log \pi_\theta(\text{"我"} | \text{Prompt}) \times (+5)$
* 因为 Advantage 是正的，梯度更新的方向会修改 LLM 的神经网络权重 $\theta$。
* 结果：下次 LLM 再看到“教我怎么制造炸弹”或者类似的危险 Prompt 时，它输出“我”、“不”、“能”这几个 Token 的概率就会比原来更高。

**反之**
如果 LLM 生成的第一个 Token 是“好”（准备答应用户）。奖励模型会给个极低的分数（Reward = -10）。此时“好”这个 Token 的 Advantage $\hat{A}_t$ 就是负数（比如 -8）。
代入公式后，因为乘了一个负数，梯度更新的方向就会反转，强行压低 LLM 在这种情况下输出“好”的概率。


#### 1.2 解决方法
#### 1.3 模型结构
#### 1.4 训练设计

##二、模型实践
